<div class="modal fade" id="deleteModalList" tabindex="-1" aria-labelledby="modalTitle" aria-hidden="true">
	<div class="modal-dialog modal-dialog-centered">
		<div class="modal-content p-2 bradius-10">
			<div class="modal-header p-3">
				<h5 class="modal-title" id="modalTitle">Eliminar lista</h5>
				<button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
			</div>
			<div class="modal-body pb-0">
				<form id="userForm" method="POST" action="{{ url_for('shatask.listas.post.delete') }}">
					<input type="hidden" name="id" value="{{ list.ID }}">
					<input type="hidden" name="nombre" value="{{ list.nombre }}">
					<input type="hidden" name="user_id" value="{{ list.user_id }}">
					<div class="ps-3 pt-3">
						<p class="text-gray-900">¿Deseas eliminar esta lista?</p>
					</div>
					<div class="modal-footer p-0">
						<button type="submit" id="submitUser" class="btn btn-outline-primario w-100 mt-3">
							<i class="bi bi-trash3"></i>
							Eliminar
						</button>
					</div>
				</form>
			</div>
		</div>
	</div>
</div>
